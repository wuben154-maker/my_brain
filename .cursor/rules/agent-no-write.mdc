---
description: Agent 内核只读护栏——禁止任何直接写图谱/画像的能力
globs: src/agent/**
alwaysApply: false
---

# Agent 写能力边界（v2 改写）

v2 主流程**下线提议收件箱**；Agent 不得静默改图谱/画像。例外与铁律如下。

## 新建节点：仅经「用户确认入库」出口

- **create（新建概念节点）**只能由 V3 用户语音确认后，经 conductor / `applyIngestDecision` 触发落库。
- **Agent 不得静默 create**；`AgentTools` **绝不**加 `saveConcept` / 直接 `persistGraphSnapshot`。

## 整理类变更：agent/curation 层 V4 授权直接 apply

- **merge / archive / link / attach / edge-migrate** 在 V4 路径由 **`autoCurate` → 调用方直接 `applyGraphMutation` + persist**。
- 这是 v2 **例外**：agent/curation 层**获授权直接 apply 整理类变更**，**不再**强制走提议收件箱。
- **禁止**任何静默 **pending 提议**（无 `saveProposal` pending）；v2 主流程不注册产审批 job（A3/C2 等关停，见 V7）。

## 记忆引擎边界（不变）

- **记忆引擎（M 系列）仍绝不写图谱/画像**；只读 transcript 做 profile 蒸馏（V5）。

## Agent 内核只读护栏（compile-time + runtime）

- `AgentTools` 只暴露读 + LLM/news 能力（`fetchNews`/`summarize`/`explain`/`readGraph`/`readProfile`；整理逻辑在 curation 模块由 pipeline 调用，**不**经 Agent 直写 storage）。
- `src/agent/**` 内核 run 路径**不得** import `StorageProvider` 做写操作；**不得**在 job 内静默 `persistGraphSnapshot`。
- curation **apply** 路径在 `lib/runAutoCuratePipeline.ts`（或等价）由 orchestration 层调用，非 Agent job 直写。
- 任何 `AgentJob.run` 只组装 `AgentRunResult{ digest, trace, ... }`，**不产 pending proposals**。
- 新增能力保留三重护栏：编译期类型 `AssertAgentToolsReadOnly` + 运行期 `assertAgentToolsReadOnly` + 源码扫描测试（`readRepoSource`）。

```ts
// ❌ 禁止
interface AgentTools { saveConcept(n: ConceptNode): Promise<void>; }
// ✅ 只读（整理 apply 在 curation pipeline，非 AgentTools 写句柄）
interface AgentTools { readGraph(): Promise<BrainGraphSnapshot>; }
```

改这里务必新增/保留「无写能力」断言测试。
