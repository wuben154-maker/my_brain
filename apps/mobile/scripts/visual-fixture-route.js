// Maestro helper — navigates to visual fixture route when implemented in dev builds.
// Falls back gracefully when route is unavailable (capture-android uses adb screencap).

console.log(`[visual-fixture] route=${CAPTURE_ROUTE}`);
