# Mobile Visual Fixtures

Mock navigation states for Maestro/adb capture used by `companion-ui-parity`.

- Registry: `app-development/specs/visual-fixtures/companion-registry.json`
- Maestro flow: `apps/mobile/.maestro/visual-capture.yaml`
- Capture output: `app-development/specs/reports/artifacts/visual/<screen-id>/capture.png`

When no device is available, the skill falls back to Static Parity Mode (reference TSX + baseline PNG).
